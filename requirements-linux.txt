# Python packages for the Linux build (locazo_linux.py), installable via pip.
Pillow>=10.0.0
python-xlib>=0.33

# Not pip-installable -- install these system packages first:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0 \
#                    gir1.2-ayatanaappindicator3-0.1 xclip xdg-utils
# Region selection and screen grabbing use GTK/GDK + Cairo (no gnome-screenshot).
